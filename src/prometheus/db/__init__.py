"""Application database: projects/documents/conversations workspace model.

Separate from prometheus.findings, which holds the older Prometheus
reasoning-chain reports (see prometheus.config.Settings.database_url vs
app_database_url).
"""
